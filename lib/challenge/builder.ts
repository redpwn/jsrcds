import type { Resource } from "../resources";
import { DepGraph } from "dependency-graph";
export class ChallengeBuilder {
  resources: Resource[] = [];
  // dependecyGraph: DepGraph = new DepGraph();
}
